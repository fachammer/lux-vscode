{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "yarn build",
            "group": "build",
            "command": "yarn run build"
        }
    ]
}